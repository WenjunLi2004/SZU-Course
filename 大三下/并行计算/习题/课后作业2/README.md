## 评分
- 3.4： 80  
- 3.7： 70  
- 3.9： 100  
- 3.A： 100  
- 3.B： 100     

**平均分： 90**

## 错题分析
### 题目 3.4 （得分：80/100，扣20分）

**题目明确要求：** “要求写出程序代码，假设每个进程需求和的值=进程号，截图测试结果。”

**你的失分原因：**

1. **缺少运行测试截图（主要扣分点）：** 题目明确要求“截图测试结果” 。但在你的答卷中，你只提供了手写的代码 ，并没有提供程序在计算机上实际编译运行后的结果截图。编程题不提供运行结果通常会被视为未完成上机验证。
    
2. **初始值设定未完全遵照题意：** 题目要求“假设每个进程需求和的值=进程号” （即 `value = rank`）。而在你的手写代码中，你写的是 `int my_val = rank + 1;` 。虽然逻辑上影响不大，但这不符合题目给定的初始条件。
    
3. **代码形式：** 老师的参考答案给出的是可以直接编译运行的完整 C++ 源码 ，而你提交的是手写片段，且在第二部分（任意数量进程）的代码结构较为零散 ，缺乏完整性验证。

---

### 题目 3.7 （得分：70/100，扣30分）

**题目明确要求：** “使用C clock函数为ping-pong程序计时... 要求写出程序代码，截图测试结果。”

**你的失分原因：**

1. **完全缺少程序代码（最严重扣分点）：** 题目明确要求编写 ping-pong 的计时程序代码并截图 （参考答案中给出了完整的代码实现 ）。但在你的答卷中，你**仅仅用文字回答了理论部分** ，完全没有提供任何代码，也没有测试结果截图。缺失了整个编程实践部分，这是扣掉30分的主要原因。
    
2. **对 `clock` 函数精度的认知存在事实错误：** * **你的回答：** “代码累积的CPU运算时间达到1个时钟周期才会给出非零运行时间，即**达到1纳秒**” 。
    
    - **参考答案/标准事实：** `clock` 函数在 Windows 下的精度约为 **15毫秒**左右，在 Linux/macOS 下约为 **1毫秒**左右 。 1纳秒（ns）的精度极高，C语言标准的 `clock()` 函数受限于 `CLOCKS_PER_SEC` 宏定义，根本达不到纳秒级精度。
        
3. **理论分析的优点：** 你对 `MPI_Wtime`（墙上时间，包含网络延迟）和 `clock`（CPU时间，不包含挂起等待时间）的区别分析得非常准确 ，这部分应该是你拿到70分的原因。